version: 2

models:
  - name: dim_artists
    columns:
      - name: artist_name_normalized
        tests: [not_null, unique]

  - name: dim_artist_payout_rules
    columns:
      - name: artist_name_normalized
        tests: [not_null]
      - name: split_mode
        tests: [not_null]

  - name: mart_lead_lifecycle
    columns:
      - name: lead_id
        tests: [not_null, unique]
      - name: lead_created_at
        tests: [not_null]
      - name: lifecycle_status
        tests: [not_null]

  - name: mart_client_lifecycle
    columns:
      - name: client_id
        tests: [not_null, unique]
      - name: lifecycle_segment
        tests: [not_null]
