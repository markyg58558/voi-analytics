version: 2

models:
  - name: dim_artists
    columns:
      - name: artist_name_normalized
        tests: [not_null, unique]

  - name: dim_artist_payout_rules
    columns:
      - name: artist_name_normalized
        tests: [not_null]
      - name: split_mode
        tests: [not_null]

  - name: fct_artist_payout_line_items
    columns:
      - name: sale_item_id
        tests: [not_null]
      - name: artist_name_normalized
        tests: [not_null]
      - name: payout_total
        tests: [not_null]

  - name: mart_payroll_weekly_artist
    columns:
      - name: pay_week_start
        tests: [not_null]
      - name: team_member_id
        tests: [not_null]

  - name: mart_payroll_artist_daily
    columns:
      - name: activity_date
        tests: [not_null]
      - name: team_member_id
        tests: [not_null]

  - name: mart_payroll_weekly_artist_snapshot
    columns:
      - name: pay_week_start
        tests: [not_null]
      - name: team_member_id
        tests: [not_null]

  - name: qa_payroll_snapshot_current_week
    columns:
      - name: pay_week_start
        tests: [not_null]
