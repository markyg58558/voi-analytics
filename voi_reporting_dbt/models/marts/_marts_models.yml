version: 2

models:
  - name: dim_artists
    columns:
      - name: artist_name_normalized
        tests: [not_null, unique]

  - name: dim_artist_payout_rules
    columns:
      - name: artist_name_normalized
        tests: [not_null]
      - name: split_mode
        tests: [not_null]
