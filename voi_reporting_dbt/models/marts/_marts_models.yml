version: 2

models:
  - name: dim_artists
    columns:
      - name: artist_name_normalized
        tests: [not_null, unique]

  - name: dim_artist_payout_rules
    columns:
      - name: artist_name_normalized
        tests: [not_null]
      - name: split_mode
        tests: [not_null]

  - name: mart_lead_completion_match
    columns:
      - name: lead_id
        tests: [not_null, unique]
      - name: lead_created_at
        tests: [not_null]
      - name: is_completed
        tests: [not_null]
