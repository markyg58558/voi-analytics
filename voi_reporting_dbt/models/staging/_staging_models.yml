version: 2

models:
  - name: stg_voi__bookings
    columns:
      - name: booking_id
        tests: [not_null]

  - name: stg_voi__deposits
    columns:
      - name: deposit_id
        tests: [not_null]
      - name: pay_week_start
        tests: [not_null]

  - name: stg_voi__sale_items
    columns:
      - name: sale_item_id
        tests: [not_null]
      - name: sale_id
        tests: [not_null]
      - name: pay_week_start
        tests: [not_null]

  - name: stg_voi__sales
    columns:
      - name: sale_id
        tests: [not_null]
      - name: pay_week_start
        tests: [not_null]

  - name: stg_voi__clients
    columns:
      - name: client_id
        tests: [not_null]

  - name: stg_ops__leads
    columns:
      - name: lead_id
        tests: [not_null]
      - name: email
        tests: [not_null]

  - name: stg_voi__team_members
    columns:
      - name: team_member_id
        tests: [not_null]
      - name: full_name
        tests: [not_null]
